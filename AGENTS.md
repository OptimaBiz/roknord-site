# Roknord Project Agent Instructions

## Scope And Precedence

These instructions apply to the entire repository.

- Respond in Russian unless the user explicitly requests another language.
- Follow the user's current task over project defaults, but never silently violate the brand, legal-safety, privacy, or deployment rules below.
- Before editing, inspect the relevant source files, shared components, and tokens. Do not infer the implementation from screenshots, generated HTML, or stale documentation.
- Keep changes narrow and reviewable unless the user explicitly asks for a broad redesign or refactor.
- Do not modify application source files for review, audit, explanation, or documentation-only tasks.
- Preserve unrelated user changes in a dirty worktree.
- Do not commit, push, deploy, or change external services without an explicit request.

## Project Reality

Roknord is a Russian-language static B2B marketing site built with Astro 6 and plain CSS/JavaScript.

- Runtime: Node.js `>=22.12.0`.
- Package manager: npm; use the committed `package-lock.json`.
- Application source: `src/`.
- Static source assets: `public/`.
- Shared shell: `src/layouts/BaseLayout.astro`.
- Routes: file-based pages under `src/pages/`.
- Reusable UI: `src/components/`.
- Design system and global styles: `src/styles/`.
- Production build: `dist/` (ignored, generated by Astro).
- Deployment: GitHub Pages builds `dist/` from `main` via `.github/workflows/deploy-pages.yml`.
- `docs/` and `roknord-dist.zip` are legacy/generated publication artifacts, not implementation sources. Do not hand-edit or regenerate them unless the task explicitly names them.
- There is no database, API route, CMS, or client framework.
- The contact form submits to Formspree from `src/components/ContactForm.astro`. Do not send test submissions or change its endpoint without explicit authorization.
- The personal-account modal is UI-only. Do not imply that authentication or an account backend exists.

`README.md` is still based on the Astro starter and may be incomplete. For current structure, inspect the code; use `CODEMAP.md` only as an orientation aid and verify its claims against the repository.

## Source Map

Inspect these files first for common changes:

- page composition: `src/pages/index.astro` and the relevant route under `src/pages/`;
- global shell and metadata: `src/layouts/BaseLayout.astro`;
- header and information architecture: `src/components/Header.astro`, `src/components/MobileMenu.astro`, `src/styles/header.css`;
- hero: `src/components/Hero.astro`, `src/components/HeroMedia.astro`, `src/styles/hero.css`;
- shared tokens: `src/styles/tokens.css`;
- shared primitives: `src/styles/utilities.css`, `src/styles/sections.css`;
- section layouts: `src/styles/content.css`, `src/styles/promo.css`;
- responsive behavior: `src/styles/responsive.css`;
- form behavior: `src/components/ContactForm.astro`;
- footer and legal navigation: `src/components/Footer.astro`.

All CSS is global even when some component-specific CSS is colocated in `.astro` files. Search for a selector before changing it and check responsive overrides.

## Required Skills And Roles

Use the smallest relevant set of repo-local skills:

- `roknord-brand` for any Roknord positioning, service, accreditation, Росаккредитация, evidence, risk, or brand task;
- `roknord-copy-rules` whenever Russian headings, body copy, CTA, service descriptions, metadata, or legal-safe wording change;
- `roknord-ui-style` for UI, Astro/CSS, layout, header, hero, cards, media blocks, or visual QA;
- `roknord-visual-system` (stored in `.agents/skills/roknord-lrqa-visual-reference/`) for visual direction, palette, composition, placeholders, and redesign work.

When delegation is explicitly requested and the configured role is available:

- `roknord-art-director` reviews visual direction and composition;
- `roknord-frontend-implementer` implements an approved Astro/CSS brief;
- `roknord-qa-guardian` reviews build, responsive behavior, navigation, copy, and brand compliance.

The root instructions and current design tokens take precedence over stale values embedded in a role configuration. Do not delegate by default.

## Brand Invariant

Рокнорд — комплексная подготовка к аккредитации и подтверждению компетентности.

Сложные процедуры. Твёрдая подготовка.

Документы, процессы, доказательства и риски — в логике проверки ФСА / Росаккредитации.

Рокнорд — независимая экспертная подготовка к процедурам Росаккредитации. Рокнорд не является государственным органом, официальным представителем Росаккредитации, сертификационным центром или продавцом гарантированного результата.

Position services around:

- независимый аудит готовности;
- карта рисков;
- проверка доказательств;
- анализ и актуализация области аккредитации;
- анализ СМК;
- проверка согласованности данных и практики с ФГИС;
- подготовка корректирующих действий;
- сопоставление требований, документов, записей и фактической практики.

## Copy And Legal Safety

Write in strict, evidence-driven Russian B2B language. Prefer concrete checks and deliverables over marketing adjectives. A useful section normally answers:

1. Что проверяем.
2. Почему это важно для процедуры.
3. Что получает заказчик.

Prefer formulations such as:

- независимый аудит готовности;
- карта рисков;
- проверка доказательной базы;
- подготовка к процедуре;
- анализ области аккредитации;
- выявление рисков до подачи;
- сопоставление документов и фактической практики;
- план корректирующих действий;
- проверка анкеты самообследования;
- анализ выборки дел;
- проверка связки «требование → процедура → запись → доказательство».

Avoid vague phrases such as `индивидуальный подход`, `опытные специалисты`, `качественно и в срок`, and `поможем пройти` unless concrete evidence immediately supports the claim.

Never use or paraphrase claims that imply guaranteed outcomes, special access, official status, or performing the regulator's function, including:

- гарантируем прохождение;
- гарантия прохождения;
- гарантия аккредитации;
- решим вопрос с Росаккредитацией;
- договоримся с экспертами;
- снимем замечания;
- закроем несоответствия;
- официальное заключение;
- официальное сопровождение;
- обеспечим положительное решение;
- аккредитация под ключ с гарантией;
- Не гарантия прохождения.

## Allowed Language

Prefer:
- независимый аудит готовности;
- карта рисков;
- проверка доказательств;
- подготовка к процедуре;
- анализ области аккредитации;
- корректирующие действия;
- сопоставление документов и фактической практики;
- проверка доказательной базы;
- выявление рисков до подачи;
- план корректирующих действий.

## Visual Direction

Roknord visual system:
- dark navy base with mint accents, white surfaces, and restrained grey neutrals;
- large dark hero;
- large typography;
- blocky editorial composition;
- image-led sections;
- sharp rectangular rhythm;
- corporate assurance feel;
- fewer dashboards;
- fewer equal cards;
- no generic SaaS/Tilda look.

Do not copy external brand assets, logos, texts, images, or exact HTML/CSS from any outside site.
Repeat the color system, composition principles, header/hero rhythm, large image placeholders, blocky solution/resource sections, and corporate assurance mood.

## Header Information Architecture

The approved header has two levels:

1. Top service row: `О компании`, `Личный кабинет`, `Карьера`, `Сотрудничество`, `Контакты`; CTA: `Разобрать задачу`.
2. Main product row: Roknord logo and only `Услуги` and `Кому помогаем` as primary items.

Dropdown `Услуги`:

- Подготовка к подтверждению компетентности
- Аудит и актуализация области аккредитации
- Корректирующие действия после несоответствий
- Подготовка к первичной аккредитации
- Специальные направления

Dropdown `Кому помогаем`:

- Испытательным лабораториям
- Медицинским лабораториям
- Органам по сертификации систем менеджмента
- Органам по сертификации продукции
- Органам инспекции
- Калибровочным лабораториям
- Провайдерам МСИ
- Органам по сертификации халяль

Do not place `О компании`, `Материалы`, or `Контакты` in the main row. `О компании` and `Контакты` belong only in the service row. Do not add `Материалы` to navigation without an explicitly approved placement. Preserve keyboard, pointer, and mobile access when editing dropdowns. Do not change existing sticky behavior unless the task explicitly requires it.

## Visual And UI Canon

The intended result is a corporate assurance/advisory site, not a generic SaaS landing page:

- dark navy header and hero mass;
- mint accents used for CTA, links, arrows, and key actions;
- white and restrained grey content surfaces;
- large Montserrat typography;
- sharp rectangular, editorial composition;
- large image/video/report blocks;
- hard grid alignment and generous spacing;
- few, purpose-specific cards rather than interchangeable card grids;
- mobile treated as a deliberate composition, not merely a compressed desktop.

Avoid blue SaaS gradients, dashboards as the primary hero object, equal 3x3 feature grids, random icon libraries, soft startup shadows, excessive rounding, decorative JavaScript, and a generic Tilda-like section stack.

Do not copy external logos, text, imagery, trademarked assets, or exact HTML/CSS. External references may inform only general composition and interaction principles.

### Tokens And Typography

`src/styles/tokens.css` is the implementation source of truth. Use existing semantic tokens before adding a token, and add a shared token before repeating a new literal. Do not hard-code color values in components merely because an older brief or role mentions a hex value.

The mandatory LRQA-derived base palette is:

```css
--lrqa-navy: #0E0B3A;
--lrqa-navy-2: #0F0B38;
--lrqa-nav: #110D34;
--lrqa-dark-mass: #0F0C37;
--lrqa-mint: #00FAB0;
--lrqa-mint-2: #00F8AF;
--lrqa-mint-3: #00F9B0;
--lrqa-white: #FFFFFF;
--lrqa-white-soft: #FEFEFE;
--lrqa-card-light: #F9F9FA;
--lrqa-card-grey: #ECECEC;
--lrqa-warning: #FED503;
--lrqa-black: #000000;
--lrqa-dark-border: #27274E;
--lrqa-muted-dark: #3A3752;
--lrqa-muted: #55536E;
--lrqa-muted-2: #73718B;
--lrqa-border: #B9B8C5;
--lrqa-border-soft: #DEDDE4;
```

Do not reintroduce `#B7FF00`, `#D7FF3F`, `#003A40`, `#002D32`, or `#F4F1EA` as approved brand colors.

Use the existing Montserrat variables: `--font-family-base`, `--font-family-heading`, `--font-body`, and `--font-heading`. Do not switch typefaces unless explicitly requested.

### Editorial Promo Pattern

Editorial, promo, report, and expert-material blocks use the shared magenta/purple frame and integrated angled label-tab. The tab belongs to the top border line; it is not a floating green badge.

Use the existing tokens and utilities:

- `--color-editorial-accent`;
- `--color-promo-border-accent`;
- `--color-promo-label-bg`;
- `--color-promo-label-text`;
- `--editorial-frame-bevel`;
- `--editorial-label-bevel`;
- `--button-bevel`;
- shared framed-card and angled-CTA classes in `src/styles/utilities.css`.

Do not apply editorial framing to every card. Preserve the semantic distinction between editorial frames, image-led audience/navigation cards, neutral cards, and deliverable/pricing/scenario cards. Filled CTA buttons use the shared two-sided angled shape.

## Implementation Rules

- Prefer semantic HTML, Astro components, shared CSS tokens, and reusable utilities.
- Keep client-side code vanilla and minimal. Do not add a framework or heavy library for a local interaction.
- Keep content data near the component when that matches the existing pattern; do not introduce a CMS/data layer without a task requiring it.
- Preserve visible focus states, keyboard navigation, labels, landmarks, alt text, and reduced-motion behavior.
- Guard hover-specific behavior with an appropriate hover media query and keep touch behavior usable.
- Reuse assets from `public/`; do not edit generated copies under `docs/`.
- Use root-relative public URLs such as `/images/...` and `/pdf/...`.
- Do not expose secrets or add `.env` files to version control.
- Treat changes to the Formspree endpoint, consent/legal pages, analytics, external scripts, and deployment workflow as sensitive scope: change them only when explicitly requested and call them out in the handoff.

## Verification And Definition Of Done

For any application/frontend change:

1. Run `npm run build` from the repository root.
2. Review build output for warnings and route failures.
3. For layout or interaction changes, inspect the affected route at desktop and mobile widths when browser tooling is available.
4. Exercise affected keyboard interactions and check for obvious overflow, clipping, and focus regressions.
5. Search changed copy for forbidden claims and verify header/navigation canon when relevant.
6. Report the files changed, checks run, and any checks that could not be run.

Documentation-only changes do not require an Astro build unless they alter executable configuration. Do not regenerate `docs/`, archives, screenshots, or production artifacts as a side effect of verification.

# Claude Instructions For Roknord

Before any work in this repository, read and follow `AGENTS.md` in full. It is the canonical project instruction for architecture, brand positioning, legal-safe copy, visual rules, implementation boundaries, and verification.

Additional operating rules:

- respond in Russian unless the user explicitly requests another language;
- inspect the relevant source, shared tokens, and components before editing;
- keep diffs narrow and preserve unrelated user changes;
- do not treat `README.md`, `CODEMAP.md`, `docs/`, screenshots, or archives as more authoritative than current source code;
- do not commit, push, deploy, submit forms, or mutate external services without an explicit request;
- if a request conflicts with the brand or legal-safety canon in `AGENTS.md`, explain the conflict and offer a compliant formulation.

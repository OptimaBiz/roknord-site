---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Обращение отправлено — Рокнорд"
  description="Подтверждение отправки обращения в Рокнорд. Мы принимаем запрос в работу и возвращаемся с первичным ответом."
>
  <main id="content">
    <section class="thank-you">
      <div class="thank-you__inner page-container">
        <div class="thank-you__panel">
          <div class="thank-you__copy">
            <p class="section__eyebrow">Обращение принято</p>
            <h1>Спасибо. Мы получили ваш запрос.</h1>
            <p>
              Сообщение передано. Мы ответим в течение рабочего дня.
            </p>
          </div>

          <div class="thank-you__next" aria-label="Что происходит дальше">
            <p class="thank-you__label">Дальше</p>
            <ul>
              <li>Изучим обращение;</li>
              <li>При необходимости уточним детали;</li>
              <li>Предложим подходящий объём работ и условия</li>
            </ul>
          </div>

          <div class="thank-you__actions">
            <a class="thank-you__button cta-angled motion-control" href="/">На главную</a>
            <a class="thank-you__link" href="/#directions">Посмотреть услуги</a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <script is:inline>
    // Сюда форма уводит только после успешной отправки — значит это и есть конверсия.
    (function () {
      var fire = function () {
        if (typeof ym === "function") ym(111293528, "reachGoal", "form_submit");
      };
      if (typeof ym === "function") fire();
      else window.addEventListener("load", fire, { once: true });
    })();
  </script>
</BaseLayout>

<style>
  .thank-you {
    position: relative;
    overflow: hidden;
    min-height: calc(100svh - var(--header-total-height, 7rem));
    padding-block: clamp(var(--space-10), 8vw, var(--space-16));
    background:
      linear-gradient(90deg, color-mix(in srgb, var(--lrqa-mint) 10%, transparent) 0 1px, transparent 1px 100%),
      linear-gradient(180deg, color-mix(in srgb, var(--lrqa-white) 4.5%, transparent) 0 1px, transparent 1px 100%),
      linear-gradient(135deg, var(--lrqa-navy) 0%, var(--lrqa-nav) 48%, var(--lrqa-dark-mass) 100%);
    background-size: 5rem 5rem, 5rem 5rem, auto;
    color: var(--lrqa-white);
  }

  .thank-you::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(100deg, transparent 0%, color-mix(in srgb, var(--lrqa-mint) 12%, transparent) 46%, transparent 68%);
    transform: translateX(-100%);
    opacity: 0.9;
  }

  .thank-you__inner {
    position: relative;
    z-index: 1;
  }

  .thank-you__panel {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(18rem, 0.52fr);
    gap: clamp(var(--space-6), 5vw, var(--space-12));
    align-items: start;
    padding: clamp(var(--space-6), 5vw, var(--space-10));
    border: 1px solid var(--color-border-strong);
    border-top: 2px solid var(--lrqa-mint);
    background: color-mix(in srgb, var(--lrqa-navy-2) 84%, transparent);
  }

  .thank-you__panel::after {
    content: "";
    position: absolute;
    right: var(--space-5);
    bottom: var(--space-5);
    width: 7.5rem;
    height: 7.5rem;
    border-right: 1px solid color-mix(in srgb, var(--lrqa-mint) 48%, transparent);
    border-bottom: 1px solid color-mix(in srgb, var(--lrqa-mint) 48%, transparent);
    pointer-events: none;
  }

  .thank-you__copy {
    display: grid;
    gap: var(--space-5);
    max-width: 52rem;
  }

  .thank-you h1 {
    margin: 0;
    font-family: var(--font-heading);
    font-size: calc(var(--type-page-title) * var(--hero-heading-scale));
    font-weight: var(--font-weight-hero-title);
    line-height: 0.98;
    letter-spacing: 0;
  }

  .thank-you__copy p:not(.section__eyebrow) {
    margin: 0;
    max-width: 42rem;
    color: var(--lrqa-border-soft);
    font-size: var(--type-lead);
    line-height: var(--line-height-relaxed);
  }

  .thank-you__next {
    display: grid;
    gap: var(--space-4);
    padding: var(--space-5);
    border: 1px solid rgba(222, 221, 228, 0.34);
    background: rgba(255, 255, 255, 0.06);
  }

  .thank-you__label {
    margin: 0;
    color: var(--lrqa-mint);
    font-size: var(--font-size-2xs);
    font-weight: var(--font-weight-semibold);
    letter-spacing: var(--letter-spacing-wide);
    text-transform: uppercase;
  }

  .thank-you__next ul {
    display: grid;
    gap: var(--space-3);
    margin: 0;
    padding: 0;
    list-style: none;
    color: var(--lrqa-border-soft);
    line-height: var(--line-height-relaxed);
  }

  .thank-you__next li {
    display: flex;
    gap: var(--space-3);
  }

  .thank-you__next li::before {
    content: "";
    flex: 0 0 0.62rem;
    width: 0.62rem;
    height: 0.62rem;
    margin-top: 0.45rem;
    background: var(--lrqa-mint);
  }

  .thank-you__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
    align-items: center;
    grid-column: 1 / -1;
  }

  .thank-you__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 3.35rem;
    padding: var(--space-3) var(--space-8);
    background: var(--lrqa-mint);
    color: var(--lrqa-black);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
  }

  .thank-you__button:hover,
  .thank-you__button:focus-visible {
    background: var(--lrqa-mint-2);
  }

  .thank-you__link {
    display: inline-flex;
    align-items: center;
    min-height: 3.35rem;
    color: var(--lrqa-white);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    border-bottom: 2px solid var(--lrqa-mint);
  }

  @media (max-width: 760px) {
    .thank-you {
      background-size: 3.4rem 3.4rem, 3.4rem 3.4rem, auto;
    }

    .thank-you__panel {
      grid-template-columns: 1fr;
      padding: var(--space-5);
    }

    .thank-you h1 {
      font-size: calc(var(--type-display) * var(--hero-heading-scale));
      line-height: 1.02;
    }

    .thank-you__actions,
    .thank-you__button,
    .thank-you__link {
      width: 100%;
    }

    .thank-you__link {
      justify-content: center;
    }
  }
</style>

<section class="pdn-hero" aria-labelledby="pdn-title">
  <div class="page-container pdn-hero__grid">
    <div class="pdn-hero__copy" data-reveal>
      <p class="pdn-label">Аудит и документы по 152‑ФЗ</p>
      <h1 id="pdn-title"><span>Аудит по 152‑ФЗ</span><span>и документы</span></h1>
      <p class="pdn-hero__lead">За 22&nbsp;000 ₽ проверим сайт, CRM, документы и сведения в Роскомнадзоре. Найдём расхождения, составим план исправлений и подготовим стандартный комплект документов под фактические процессы компании.</p>

      <ul class="pdn-hero__benefits" aria-label="Что входит в услугу">
        <li>Карта рисков и приоритетный план исправлений</li>
        <li>Политика, согласия и внутренние документы</li>
        <li>Проект уведомления в Роскомнадзор или изменений к нему</li>
      </ul>

      <div class="pdn-hero__price">
        <strong>22&nbsp;000 ₽</strong>
        <span>Фиксированная стоимость стандартного объёма. Срок — 48 часов после получения исходных данных.</span>
      </div>

      <div class="pdn-actions">
        <a class="button button-primary cta-angled pdn-hero__primary" href="#audit-form" data-pdn-goal="pdn_audit_hero_cta">Заказать аудит и документы →</a>
        <a class="pdn-hero__scope-link" href="#result-title">Что входит в 22&nbsp;000 ₽ →</a>
      </div>
    </div>

    <figure class="pdn-hero__data-route" data-reveal>
      <img
        src="/images/pdn-audit/shema-marshruta-personalnyh-dannyh-152-fz.webp"
        width="1536"
        height="1024"
        alt="Схема маршрута персональных данных: посетитель, форма сайта, CRM, менеджер, хранение и удаление"
        loading="eager"
        fetchpriority="high"
        decoding="async"
        sizes="(max-width: 64rem) calc(100vw - 2rem), (max-width: 90rem) 46vw, 620px"
      />
    </figure>
  </div>
</section>

<style>
  .pdn-hero {
    padding-block: clamp(3rem, 5vw, 5.5rem);
  }

  .page-container.pdn-hero__grid {
    max-width: 87.5rem !important;
    grid-template-columns: minmax(0, 1.08fr) minmax(0, .92fr);
    gap: clamp(2.5rem, 4vw, 3.5rem);
    align-items: center;
  }

  .pdn-hero__copy {
    gap: clamp(1rem, 1.35vw, 1.35rem);
  }

  .pdn-hero h1 {
    width: 100%;
    max-width: none;
    font-size: clamp(3.6rem, 4.75vw, 5rem);
    line-height: .98;
    letter-spacing: -.052em;
  }

  .pdn-hero h1 span {
    display: block;
    white-space: nowrap;
  }

  .pdn-hero__lead {
    max-width: 44rem;
    margin: 0;
    font-size: clamp(1.05rem, 1.22vw, 1.25rem);
    line-height: 1.48;
  }

  .pdn-hero__benefits {
    display: grid;
    gap: .65rem;
    margin: .2rem 0;
    padding: 0;
    list-style: none;
  }

  .pdn-hero__benefits li {
    position: relative;
    padding-left: 1.75rem;
    font-weight: 600;
    line-height: 1.35;
  }

  .pdn-hero__benefits li::before {
    content: '✓';
    position: absolute;
    left: 0;
    top: 0;
    color: var(--color-accent);
    font-weight: 800;
  }

  .pdn-hero__price {
    display: grid;
    grid-template-columns: auto minmax(0, 24rem);
    align-items: center;
    gap: clamp(1rem, 1.8vw, 1.75rem);
    width: fit-content;
    padding: 1rem 1.25rem;
    border: 1px solid var(--color-border-strong);
    background: var(--color-surface-card-dark);
  }

  .pdn-hero__price strong {
    color: var(--color-accent);
    font: 700 clamp(1.8rem, 2.3vw, 2.35rem)/1 var(--font-family-mono);
    white-space: nowrap;
  }

  .pdn-hero__price span {
    color: var(--color-text-muted);
    font-size: var(--font-size-sm);
    line-height: 1.45;
  }

  .pdn-actions {
    align-items: center;
    gap: clamp(1rem, 2vw, 1.75rem);
  }

  .pdn-hero__primary {
    min-height: 3.5rem;
    padding: .95rem 1.7rem;
    font-size: 1rem;
  }

  .pdn-hero__scope-link {
    color: var(--color-text-primary);
    font-weight: 600;
    text-decoration: underline;
    text-decoration-color: var(--color-accent);
    text-underline-offset: .3em;
  }

  .pdn-hero__scope-link:hover {
    color: var(--color-accent);
  }

  .pdn-hero__data-route {
    display: grid;
    width: min(100%, 38.75rem);
    min-width: 0;
    margin: 0;
    justify-self: end;
  }

  .pdn-hero__data-route img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 3 / 2;
    object-fit: contain;
  }

  @media (max-width: 72rem) and (min-width: 64.01rem) {
    .pdn-hero h1 {
      font-size: clamp(3.1rem, 4.5vw, 3.6rem);
    }

    .pdn-hero__price {
      grid-template-columns: 1fr;
      gap: .65rem;
    }
  }

  @media (max-width: 64rem) {
    .page-container.pdn-hero__grid {
      grid-template-columns: 1fr;
      gap: clamp(2.5rem, 6vw, 4rem);
    }

    .pdn-hero__copy {
      max-width: 48rem;
    }

    .pdn-hero h1 {
      font-size: clamp(3.5rem, 8vw, 4.75rem);
    }

    .pdn-hero__data-route {
      width: min(100%, 60rem);
      justify-self: start;
    }
  }

  @media (max-width: 40rem) {
    .pdn-hero {
      padding-block: 2.75rem 3.5rem;
    }

    .page-container.pdn-hero__grid {
      gap: 2.5rem;
    }

    .pdn-hero__copy {
      gap: 1rem;
    }

    .pdn-hero h1 {
      font-size: clamp(2.15rem, 10.4vw, 3.5rem);
      line-height: 1.02;
      letter-spacing: -.045em;
    }

    .pdn-hero__lead {
      font-size: 1rem;
    }

    .pdn-hero__benefits li {
      padding-left: 1.5rem;
      font-size: .95rem;
    }

    .pdn-hero__price {
      grid-template-columns: 1fr;
      gap: .65rem;
      width: 100%;
      padding: 1rem;
    }

    .pdn-actions {
      display: grid;
      justify-items: start;
    }

    .pdn-actions .pdn-hero__primary {
      width: 100%;
      text-align: center;
    }

    .pdn-hero__data-route {
      width: 100%;
      margin-inline: 0;
    }

    .pdn-hero__data-route img {
      width: 100%;
      max-width: 100%;
      transform: none;
    }
  }
</style>
